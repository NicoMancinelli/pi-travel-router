# Forward DNS to AdGuard Home (127.0.0.1:5335)
# Install to: /etc/dnsmasq.d/adguard.conf — only when ENABLE_ADGUARD=1
# Replaces dnsmasq-dot.conf if both features were enabled
no-resolv
server=127.0.0.1#5335
