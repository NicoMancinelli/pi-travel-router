# IP forwarding for Tailscale subnet routing
# Install to: /etc/sysctl.d/99-tailscale.conf
# Apply: sysctl -p /etc/sysctl.d/99-tailscale.conf

net.ipv4.ip_forward = 1
net.ipv6.conf.all.forwarding = 1
