# radvd.conf — Router Advertisement for AP clients on uap0
# Advertises an IPv6 prefix so AP clients can use SLAAC autoconfiguration.
# The prefix is derived from the delegated prefix received by the uplink.
# DHCPv6-PD (prefix delegation) is handled by the 70-dhcpv6-uplink dispatcher.

interface uap0 {
    AdvSendAdvert on;
    MinRtrAdvInterval 30;
    MaxRtrAdvInterval 100;
    # AdvDefaultLifetime 0: clients will not install a default route via this router
    # unless DHCPv6-PD has delegated a prefix to uap0. This prevents false default
    # routes when the uplink has not provided a prefix yet.
    AdvDefaultLifetime 0;
    prefix ::/64 {
        AdvOnLink on;
        AdvAutonomous on;
        AdvRouterAddr on;
    };
};
